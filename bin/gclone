#!/usr/bin/env sh

repo=$1
dirname="$(basename $repo)"

git clone --bare git@github.com:$repo $dirname
cd $dirname
git config --local --add remote.origin.fetch "+refs/heads/*:refs/remotes/origin/*"
git fetch
cd -
