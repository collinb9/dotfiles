# Essential CLI tools for Debian/Ubuntu
# This is a minimal package list for core functionality
# Arch users can ignore this file

# Version control
git

# Editor
neovim

# Terminal multiplexer
tmux

# Shell
zsh

# Fuzzy finder
fzf

# Modern CLI tools
ripgrep
fd-find
bat

# Network tools
curl
wget

# Build tools (for some vim/nvim plugins)
build-essential

# Python development
python3
python3-pip
pylint

# Lua development (for nvim config)
luarocks
luacheck

# AWS CLI (optional - comment out if not needed)
# awscli

# Node.js (for some nvim LSP servers and AI tools)
nodejs
npm

# Note: ruff not in standard Debian repos for older versions
# Will be installed via pipx during setup

# Note: Rust toolchain (rustup) will be installed via curl during setup
# Required for: uv, stylua, cfn-lsp-extra, tombi
